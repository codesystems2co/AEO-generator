# PHASE6_REAL_SITE_TESTED — 2026-09-17

**UI:** http://2.28.106.22:9012/  
**API:** http://2.28.106.22:8642/  
**Order:** S00247 · consume SoT `https://arkiphere.cloud`  
**Key:** redacted `AEO-evpx…`

## BEFORE (keep on reset)

| File | Shows |
|------|--------|
| `BEFORE/01-order-s00247.png` | Portal order S00247, product 109, hostname on the order |
| `BEFORE/02-arkiphere-cloud-home.png` | Real shop `arkiphere.cloud` homepage |
| `BEFORE/03-wizard-blocked.png` | Gate: Sin pedido + Adquirir el pack |
| `BEFORE/04-wizard-connect.png` | Step 1 Conexión, platform icons, site `arkiphere.cloud` |
| `BEFORE/05-ce-connect-page.png` | CE shop connection page for S00247 / arkiphere.cloud |

## AFTER

| File | Shows |
|------|--------|
| `AFTER/01-wizard-connect-done.png` | Typed Odoo user `thedeployer777`, connection ready, Continuar enabled |
| `AFTER/03-arkiphere-cloud-home.png` | Same real hostname after Google step (inject did not rewrite homepage) |

Google solved is in `api/google_autofix.json` (`step_complete: true` on `https://arkiphere.cloud`). Pack tree is `api/packs_generate.json` (Ollama + hostname graph). Order line is `api/order_line_s00247.json`.

## 403 anti-DevTools

```
POST /api/packs/generate     url=https://evil.example     → 403 Usa arkiphere.cloud
POST /api/wizard/google/autofix site_url=https://evil.example → 403
POST /api/google/readiness   site_url=https://www.example.com → 403
```

Bodies: `api/403_*.json`.

## Connection path

1. Customer types platform + DB/user/API key in Optimizator step 1 (not generated by Arkiphere).  
2. Generator stores an encrypted local mirror (`/app/data/connections.json`).  
3. Generator POSTs `/aeo/connection/http` (CE 404 today) and/or XML-RPC `aeo_connect_*` on the Optimizator sale.order.line.  
4. Line 351 after typed save: platform `odoo`, shop `https://arkiphere.cloud`, login `thedeployer777`, database `osh`, state `registered`, `aeo_connect_has_secret: true`.

Revoke clears local mirror and attempts CE `/aeo/connection/revoke/http` plus line secret clear.

## Inject

`api/inject.json`: Core down → direct Odoo XML-RPC with the typed key → `Odoo authentication failed`. Not faked.

CE `/aeo/connection/http` still needs to land on arkiphere_ce so AEO can write the cipher without a portal session.

## PDF section (update)

Add a Phase 6 real-site page to `PHASE6_GENERAL_PACK_TESTED.pdf` with:

1. BEFORE homepage arkiphere.cloud  
2. Wizard step 1 platform icons  
3. Order line `aeo_connect_*`  
4. 403 mismatch JSON  
5. Google Completo on arkiphere.cloud  
6. Ollama pack resume grounded on that host
