NEW Agent — Stage · Git Fork+push AEO-generator (Phase 6 card I). ONE Agent only. Do not touch arkiphere_ce Phase 6 BEFORE epic.

Fork ALREADY exists: https://github.com/codesystems2co/AEO-generator.git (fork of gwrxuk/AEO-generator).

On `/root/anyapp-github-build/AEO-generator`:

1) `git remote add fork https://github.com/codesystems2co/AEO-generator.git` (or set-url). No PAT in URL. NEVER push origin/gwrxuk.

2) Branch `arkiphere-optimizator`. Commit local Optimizator work in rich feat/docs commits covering: wizard gate Adquirir-only + site value-only, entitlement consume, packs/GSC/branding Phases 3–5, lifecycle strip, hostname UI. Note API SoT 403 GAP honestly if still open — do not block.

3) `git push -u fork arkiphere-optimizator`

4) Evidence: `_bmad-output/2026-09-17_phase6_final/GIT_FORK_TESTED.md` + note in READY with fork URL + SHAs + “ACTION FOR ALEX: rotate compromised CE PAT if not done”.

5) Exclude .bak .env secrets client_secret* buildstamps node_modules.

Prove `git remote -v` + `git log fork/arkiphere-optimizator --oneline | head` before READY. Do not ask. Do not start Phase 6 Conexión/Postgres here.
